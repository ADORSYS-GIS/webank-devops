apiVersion: v1
kind: Pod
metadata:
  name: test-connection
spec:
  containers:
  - name: curl
    image: curlimages/curl
    command:
      - "curl"
      - "http://{{ .Release.Name }}:8080/actuator/health"
